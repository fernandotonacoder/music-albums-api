# Local development database credentials example
# To use: cp .env.example .env (then edit .env with your values)
# Do not delete this file - it's a reminder
POSTGRES_USER=dev
POSTGRES_PASSWORD=changeme
POSTGRES_DB=albums
POSTGRES_PORT=5433
