# ============================================================================
# Music Albums API - Azure DevOps Pipeline
# ============================================================================
#
# PREREQUISITES (Azure DevOps project setup):
#   1. Service Connection: azure-service-connection
#   2. Variable groups:
#      - music-albums-dev
#      - music-albums-prod
#   3. Each group should define:
#      - RESOURCE_GROUP, LOCATION, BASE_NAME, aspNetCoreEnvironment
#      - pg-admin-login, pg-admin-password, jwt-key, api-key (mark as secrets)
#
# PIPELINE RUNTIME:
#   - Choose parameter: targetEnvironment (dev | prod)
#   - Choose parameter: deployInfra (default false)
#   - Pipeline loads matching variable group automatically
#
# ============================================================================

parameters:
  - name: targetEnvironment
    displayName: Target environment
    type: string
    default: dev
    values:
      - dev
      - prod
  - name: deployInfra
    displayName: Deploy infrastructure
    type: boolean
    default: false

trigger:
  branches:
    include:
      - main
  paths:
    include:
      - src/**
      - Dockerfile
      - infra/**

pr:
  branches:
    include:
      - main
  paths:
    include:
      - src/**
      - Dockerfile
      - infra/**

pool:
  vmImage: ubuntu-latest

variables:
  - name: imageRepository
    value: fernandotonacoder/music-albums-api
  - name: containerRegistry
    value: github-ghcr
  - name: dockerfilePath
    value: '$(Build.SourcesDirectory)/Dockerfile'
  - name: tag
    value: '$(Build.BuildId)'
  - name: location
    value: 'westeurope'
  - name: baseName
    value: 'music-albums'
  - name: aspNetCoreEnvironment
    value: 'Development'
  - ${{ if eq(parameters.targetEnvironment, 'dev') }}:
    - group: music-albums-dev
    - name: deploymentEnvironmentName
      value: Development
  - ${{ if eq(parameters.targetEnvironment, 'prod') }}:
    - group: music-albums-prod
    - name: deploymentEnvironmentName
      value: Production
  - name: containerAppName
    value: '$(baseName)-api-${{ parameters.targetEnvironment }}'

stages:
  # ============================================================================
  # Build Stage
  # ============================================================================
  - stage: Build
    displayName: Build and Push
    jobs:
      - job: Build
        displayName: Build Docker Image
        variables:
          DOCKER_BUILDKIT: 1
        steps:
          - task: Docker@2
            displayName: Build Docker image
            inputs:
              containerRegistry: $(containerRegistry)
              repository: '$(imageRepository)'
              command: build
              Dockerfile: '$(dockerfilePath)'
              arguments: '--label "org.opencontainers.image.source=https://github.com/fernandotonacoder/music-albums-api"'
              tags: |
                $(tag)
                latest

          - task: Docker@2
            displayName: Push to GitHub Container Registry
            inputs:
              containerRegistry: $(containerRegistry)
              repository: '$(imageRepository)'
              command: push
              tags: |
                $(tag)
                latest

  # # ============================================================================
  # # Test Stage
  # # ============================================================================
  # - stage: Test
  #   displayName: 'Run Tests'
  #   dependsOn: Build
  #   jobs:
  #     - job: UnitTests
  #       displayName: 'Unit Tests'
  #       steps:
  #         - task: UseDotNet@2
  #           displayName: 'Use .NET SDK'
  #           inputs:
  #             packageType: 'sdk'
  #             version: '8.x'

  #         - task: DotNetCoreCLI@2
  #           displayName: 'Restore packages'
  #           inputs:
  #             command: 'restore'
  #             projects: '**/*.csproj'

  #         - task: DotNetCoreCLI@2
  #           displayName: 'Run unit tests'
  #           inputs:
  #             command: 'test'
  #             projects: '**/tests/**/*.csproj'
  #             arguments: '--configuration Release --no-restore'

  # ============================================================================
  # Deploy Infrastructure Stage (optional - run manually or on infra changes)
  # ============================================================================
  - stage: DeployInfra
    displayName: Deploy Infrastructure
    dependsOn: Build
    condition: and(succeeded(), eq('${{ parameters.deployInfra }}', 'true'), eq(variables['Build.Reason'], 'Manual'))
    jobs:
      - job: DeployBicep
        displayName: Deploy Bicep Template
        steps:
          - task: AzureCLI@2
            displayName: Ensure resource group exists
            inputs:
              azureSubscription: azure-service-connection
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                az group create \
                  --name '$(RESOURCE_GROUP)' \
                  --location '$(location)'

          - task: AzureCLI@2
            displayName: Deploy infrastructure
            inputs:
              azureSubscription: azure-service-connection
              scriptType: bash
              scriptLocation: inlineScript
              inlineScript: |
                az deployment group create \
                  --resource-group '$(RESOURCE_GROUP)' \
                  --template-file infra/main.bicep \
                  --parameters \
                    location='$(location)' \
                    baseName='$(baseName)' \
                    deploymentSuffix='${{ parameters.targetEnvironment }}' \
                    aspNetCoreEnvironment='$(aspNetCoreEnvironment)' \
                    postgresAdminLogin='$(pg-admin-login)' \
                    postgresAdminPassword='$(pg-admin-password)' \
                    jwtKey='$(jwt-key)' \
                    apiKey='$(api-key)' \
                    imageTag='$(tag)'

  # ============================================================================
  # Deploy Application Stage
  # ============================================================================
  - stage: DeployApp
    displayName: Deploy Application
    dependsOn:
      - Build
      - DeployInfra
    condition: |
      and(
        not(failed('Build')),
        not(failed('DeployInfra')),
        ne(variables['Build.Reason'], 'PullRequest')
      )
    jobs:
      - deployment: DeployToAzure
        displayName: Deploy to Container Apps
        environment: $(deploymentEnvironmentName)
        strategy:
          runOnce:
            deploy:
              steps:
                - checkout: self

                - task: AzureCLI@2
                  displayName: Update Container App
                  inputs:
                    azureSubscription: azure-service-connection
                    scriptType: bash
                    scriptLocation: inlineScript
                    inlineScript: |
                      az containerapp update \
                        --name '$(containerAppName)' \
                        --resource-group '$(RESOURCE_GROUP)' \
                        --image 'ghcr.io/$(imageRepository):$(tag)'

                - task: AzureCLI@2
                  displayName: Verify deployment
                  inputs:
                    azureSubscription: azure-service-connection
                    scriptType: bash
                    scriptLocation: inlineScript
                    inlineScript: |
                      # Wait for deployment to stabilize
                      sleep 30

                      # Get the app URL and check health
                      APP_URL=$(az containerapp show \
                        --name '$(containerAppName)' \
                        --resource-group '$(RESOURCE_GROUP)' \
                        --query 'properties.configuration.ingress.fqdn' -o tsv)

                      echo "Checking health at https://$APP_URL/_health"
                      curl -f "https://$APP_URL/_health" || exit 1
                      echo 'Deployment successful!'
